import { expect } from "chai";

describe('Index Test', function() {
    it('should always pass or something is horribly wrong with the known universe', () => {
        expect(true).to.equal(true);
    });
})